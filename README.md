# typescript-crud
This project is a simple User Management CRUD application built using TypeScript, HTML, and CSS. It allows users to load data, add new users, edit existing users, delete users, and refresh data in a clean table format.  The main goal of this project is to understand TypeScript classes, interfaces, enums, DOM manipulation, and CRUD operations
